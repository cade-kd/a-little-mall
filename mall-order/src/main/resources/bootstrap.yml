spring:
  application:
    name: mall-order
  profiles:
    active: dev

  cloud:
    nacos:
      username: nacos
      password: nacosnacos007_ac
      config:
        enabled: true # 如果不想使用 Nacos 进行配置管理，设置为 false 即可
        server-addr: 43.138.129.175:8848 # Nacos Server 地址
        namespace: dev # 命名空间id
        group: A-LITTLE-MALL # 组，默认为 DEFAULT_GROUP
        file-extension: yaml # 配置内容的数据格式，默认为 properties
